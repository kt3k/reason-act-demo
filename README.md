# reason-act-demo

This demo implements ReAct (Reason+Act) pattern in TypeScript. The demo uses OpenAI SDK for LLM call, but it can be replaced with any LLM.

This demo doesn't use framework for implementing ReAct, but only uses TypeScript and custom prompt.

## License

MIT
